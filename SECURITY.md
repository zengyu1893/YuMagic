# 安全说明

## 凭据处理

- API Key、访问令牌、密码和其他授权信息只保存在本机设置或环境变量中。
- 不要把 `.env`、设置 JSON、日志、临时上传、生成记录或真实凭据提交到 Git。
- 发布前请运行敏感信息扫描；发现已经提交的凭据时，应先立即撤销或轮换凭据，再处理仓库历史。

## 漏洞反馈

如果发现可能影响用户数据或凭据安全的问题，请通过私密渠道联系项目维护者，避免在公开 Issue 中粘贴密钥、日志或个人数据。反馈内容应只包含复现问题所需的最少信息。
